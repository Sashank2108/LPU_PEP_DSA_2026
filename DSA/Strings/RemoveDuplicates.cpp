    // Remove Dupilcates
    /* string str = "HhHHeeelloooowW";
    int n = str.length();
    string newStr = "";
    for(int i=0;i<n;i++){
        if(newStr.find(str[i])==-1)
            newStr += str[i];
    }
    cout << newStr; */